apiVersion: v1
kind: Service
metadata:
  name: gridcapa-postgresql
  labels:
    app: gridcapa
    component: postgresql
    env: azure-common
spec:
  type: ClusterIP
  selector:
    app: gridcapa
    component: postgresql
    env: azure-common
  ports:
    - port: 5432
