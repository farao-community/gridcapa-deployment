apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: core-valid-cbcora-data-bridge-storage
  labels:
    app: gridcapa
    component: data-bridge-storage
    region: core
    process: valid
spec:
  storageClassName: azure-storage-class
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 16M
