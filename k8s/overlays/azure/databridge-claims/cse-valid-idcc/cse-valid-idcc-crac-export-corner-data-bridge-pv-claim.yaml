apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cse-valid-idcc-crac-export-corner-data-bridge-storage
  labels:
    app: gridcapa
    component: data-bridge-storage
    region: cse
    process: valid-idcc
    corner: export
spec:
  storageClassName: azure-storage-class
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 16M
