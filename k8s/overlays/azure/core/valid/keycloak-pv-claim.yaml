apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: keycloak-storage
  labels:
    app: gridcapa
    component: keycloak
spec:
  storageClassName: gridcapa-storage-class
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi