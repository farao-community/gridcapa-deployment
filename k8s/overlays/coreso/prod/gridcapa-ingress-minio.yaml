apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  annotations:
    kubernetes.io/ingress.class: kommander-traefik
  creationTimestamp: null
  name: gridcapa-minio
  namespace: default
spec:
  routes:
    - kind: Rule
      match: Host(`gridcapa.ingress-gridcapa-vaas.rh.1648.local`) && PathPrefix(`/minio`)
      services:
        - kind: Service
          name: gridcapa-minio
          namespace: default
          port: 9000
