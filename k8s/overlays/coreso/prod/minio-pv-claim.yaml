apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gridcapa-coreso-minio-storage
  labels:
    app: gridcapa
    component: minio
    env: coreso-prod
spec:
  storageClassName: vsphere-raw-block-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 80Gi