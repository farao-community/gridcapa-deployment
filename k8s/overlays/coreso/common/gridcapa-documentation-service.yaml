apiVersion: v1
kind: Service
metadata:
  name: gridcapa-documentation
  labels:
    app: gridcapa
    component: documentation
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: gridcapa
    component: documentation
