apiVersion: v1
kind: Service
metadata:
  name: ftp-server
  labels:
    app: gridcapa
    component: ftp-server
spec:
  type: ClusterIP
  selector:
    app: gridcapa
    component: ftp-server
  ports:
    - port: 21
      targetPort: 21
      protocol: TCP
