apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ftp-storage
  labels:
    app: gridcapa
    component: ftp-storage
spec:
  storageClassName: gridcapa-storage-class
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi