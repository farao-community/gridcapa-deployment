kind: ConfigMap
apiVersion: v1
metadata:
  name: cse-d2cc-glsk-data-bridge-configmap
  labels:
    app: gridcapa
    component: gridcapa-glsk-data-bridge-configmap
    region: cse
    process: d2cc
data:
  application.yml: |-
    data-bridge:
      target-process: CSE_D2CC
      file-type: GLSK
      file-regex: (?<year>[0-9]{4})(?<month>[0-9]{2})(?<day>[0-9]{2})_(?<hour>[0-9]{2})(?<minute>[0-9]{2}).*.(xml|XML)
      time-validity: hourly
      remote-file-regex:
        - ".*.zip"
        - "[0-9]{8}_[0-9]{4}.*.(xml|XML)"
      sources:
        ftp:
          active: true
          host: ${GRIDCAPA_FTP_HOST}
          port: 21
          username: ${GRIDCAPA_FTP_USER}
          password: ${GRIDCAPA_FTP_PASSWORD}
          base-directory: ${GRIDCAPA_FTP_ROOT_DIRECTORY}/cse/d2cc/glsks
          polling-delay-in-ms: 5000
      sinks:
        minio:
          url: http://gridcapa-minio:9000
          access-key: ${GRIDCAPA_MINIO_USER}
          secret-key: ${GRIDCAPA_MINIO_PASSWORD}
          bucket: gridcapa
          base-directory: /CSE/D2CC/GLSKs
