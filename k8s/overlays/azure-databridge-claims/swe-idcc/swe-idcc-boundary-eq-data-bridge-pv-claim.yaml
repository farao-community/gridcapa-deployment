apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: swe-idcc-boundary-eq-data-bridge-storage
  labels:
    app: gridcapa
    component: data-bridge-storage
    region: swe
    process: idcc
spec:
  storageClassName: azure-storage-class
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 16M
