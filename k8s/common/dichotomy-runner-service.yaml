apiVersion: v1
kind: Service
metadata:
  name: dichotomy-runner
  labels:
    app: gridcapa
    component: dichotomy-runner
spec:
  type: ClusterIP
  selector:
    app: gridcapa
    component: dichotomy-runner
  ports:
    - port: 80
      targetPort: 8080
      name: http
