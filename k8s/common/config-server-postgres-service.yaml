apiVersion: v1
kind: Service
metadata:
  name: config-server-postgres
  labels:
    app: gridcapa
    component: config-server-postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
  selector:
    app: gridcapa
    component: config-server-postgres