kind: ConfigMap
apiVersion: v1
metadata:
  name: cse-export-idcc-task-notification-server-configmap
  labels:
    app: gridcapa-task-notification-server
    component: gridcapa-task-notification-server
    region: cse
    process: export-idcc
data:
  application.yml: |-
    spring:
      cloud:
        stream:
          bindings:
            input:
              destination: cse-export-idcc-task-updated
              group: cse-export-idcc-task-notification
          rabbit:
            default:
              consumer:
                auto-bind-dlq: true
                max-attempts: 1
        config:
          enabled: false
      rabbitmq:
        addresses: gridcapa-rabbitmq:5672
        username: ${RABBITMQ_USER}
        password: ${RABBITMQ_PASSWORD}
