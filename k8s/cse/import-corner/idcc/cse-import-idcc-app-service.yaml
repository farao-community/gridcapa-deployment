apiVersion: v1
kind: Service
metadata:
  name: cse-import-idcc-gridcapa-app
  labels:
    app: gridcapa
    component: gridcapa-app
    region: cse
    process: idcc
spec:
  type: ClusterIP
  selector:
    app: gridcapa
    component: gridcapa-app
    region: cse
    process: idcc
  ports:
    - port: 80
      targetPort: 80
      name: http
