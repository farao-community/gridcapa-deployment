apiVersion: v1
kind: Service
metadata:
  name: cse-import-idcc-exporter
  labels:
    app: gridcapa
    component: export
    region: cse
    process: idcc
spec:
  type: ClusterIP
  selector:
    app: gridcapa
    component: export
    region: cse
    process: idcc
  ports:
    - port: 80
      targetPort: 8080
      name: http
