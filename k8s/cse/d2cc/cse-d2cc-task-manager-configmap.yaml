kind: ConfigMap
apiVersion: v1
metadata:
  name: cse-d2cc-task-manager-configmap
  labels:
    app: gridcapa
    component: task-manager-configmap
    region: cse
    process: d2cc
data:
  application.yml: |-
    spring:
      datasource:
        url: jdbc:postgresql://faraopostgres.postgres.database.azure.com:5432/cse_d2cc_tasks
        driverClassName: org.postgresql.Driver
        username: ${POSTGRES_USER}
        password: ${POSTGRES_PASSWORD}
        sslmode: require
      jpa:
        hibernate:
          ddl-auto: update
      rabbitmq:
        host: gridcapa-rabbitmq
        port: 5672
        username: ${RABBITMQ_DEFAULT_USER}
        password: ${RABBITMQ_DEFAULT_PASS}
      cloud:
        stream:
          bindings:
            task-updated:
              binder: rabbit
              destination: cse-d2cc-task-updated

    task-server:
      minio:
        connect:
          url: http://gridcapa-minio:9000/
          access-key: ${MINIO_ACCESS_KEY}
          secret-key: ${MINIO_SECRET_KEY}
        notification:
          exchange: gridcapa.minio.events
          queue: cse-d2cc-task-manager
      process:
        tag: CSE_D2CC
        timezone: CET
        inputs:
          - CGM
          - CRAC
          - GLSK
          - TARGET-CH
          - NTC
          - NTC-RED
          - VULCANUS
