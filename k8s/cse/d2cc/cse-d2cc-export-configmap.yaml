kind: ConfigMap
apiVersion: v1
metadata:
  name: cse-d2cc-export-configmap
  labels:
    app: gridcapa
    component: export
    region: cse
    process: d2cc
data:
  application.yml: |-
    server:
      port: 8080
    spring:
      application:
        name: cse-d2cc-export
      rabbitmq:
        host: gridcapa-rabbitmq
        port: 5672
        username: ${RABBITMQ_USER}
        password: ${RABBITMQ_PASSWORD}
      cloud:
        stream:
          default:
            group: cse-d2cc-outputs
          bindings:
            consumeTaskDtoUpdate-in-0:
              destination: cse-d2cc-task-updated
          rabbit:
            default:
              consumer:
                auto-bind-dlq: true
                max-attempts: 1
            bindings:
              consumeTaskDtoUpdate-in-0:
                consumer:
                  binding-routing-key: status
        function:
          definition: consumeTaskDtoUpdate

    task-manager:
      base-url: http://cse-d2cc-task-manager
    ftp:
      host: ${SOURCE_HOST}
      port: 21
      access-key: ${SOURCE_USER}
      secret-key: ${SOURCE_PASSWORD}
      remote-relative-destination-directory: cse/d2cc/outputs
