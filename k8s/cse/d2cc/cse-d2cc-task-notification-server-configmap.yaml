kind: ConfigMap
apiVersion: v1
metadata:
  name: cse-d2cc-task-notification-server-configmap
  labels:
    app: gridcapa
    component: task-notification-server
    region: cse
    process: d2cc
data:
  application.yml: |-
    spring:
      cloud:
        stream:
          bindings:
            input:
              destination: cse-d2cc-task-updated
              group: cse-d2cc-task-notification
          rabbit:
            default:
              consumer:
                auto-bind-dlq: true
                max-attempts: 1
        config:
          enabled: false
      rabbitmq:
        addresses: gridcapa-rabbitmq:5672
        username: ${RABBITMQ_USER}
        password: ${RABBITMQ_PASSWORD}

    websocket:
      consumer:
        threads: 2
