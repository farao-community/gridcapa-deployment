apiVersion: v1
kind: Service
metadata:
  name: cse-d2cc-postgres
  labels:
    app: gridcapa
    component: postgres
    region: cse
    process: d2cc
spec:
  type: ClusterIP
  ports:
    - port: 5432
  selector:
    app: gridcapa
    component: postgres
    region: cse
    process: d2cc