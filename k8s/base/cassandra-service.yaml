apiVersion: v1
kind: Service
metadata:
  name: cassandra
  labels:
    app: gridcapa
    component: cassandra
spec:
  clusterIP: None
  ports:
    - port: 7000
      name: intra-node-communication
    - port: 7001
      name: tls-intra-node-communication
    - port: 9042
      name: cql
  selector:
    app: gridcapa
    component: cassandra