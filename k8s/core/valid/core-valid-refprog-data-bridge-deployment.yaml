kind: Deployment
apiVersion: apps/v1
metadata:
  name: core-valid-refprog-data-bridge
  labels:
    app: gridcapa
    component: gridcapa-refprog-data-bridge
    region: core
    process: valid

spec:
  selector:
    matchLabels:
      app: gridcapa
      component: gridcapa-refprog-data-bridge
      region: core
      process: valid
  template:
    metadata:
      labels:
        app: gridcapa
        component: gridcapa-refprog-data-bridge
        region: core
        process: valid
    spec:
      containers:
        - name: core-valid-refprog-data-bridge
          image: docker.io/farao/gridcapa-data-bridge:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: GRIDCAPA_SFTP_USER
              valueFrom:
                secretKeyRef:
                  name: gridcapa-sftp-credentials
                  key: sftp-user
            - name: GRIDCAPA_SFTP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gridcapa-sftp-credentials
                  key: sftp-password
            - name: GRIDCAPA_MINIO_USER
              valueFrom:
                secretKeyRef:
                  name: gridcapa-minio-credentials
                  key: minio-access-key
            - name: GRIDCAPA_MINIO_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gridcapa-minio-credentials
                  key: minio-secret-key
          volumeMounts:
            - mountPath: /config/application.yml
              subPath: application.yml
              name: core-valid-refprog-data-bridge-configmap-volume
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 25
            periodSeconds: 15
            timeoutSeconds: 2
            failureThreshold: 3
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 35
            timeoutSeconds: 2
            periodSeconds: 15
            failureThreshold: 3
            successThreshold: 1
          resources:
            requests:
              cpu: "0.1"
              memory: "384Mi"
            limits:
              cpu: "0.5"
              memory: "384Mi"
      restartPolicy: Always
      volumes:
        - name: core-valid-refprog-data-bridge-configmap-volume
          configMap:
            name: core-valid-refprog-data-bridge-configmap
