apiVersion: v1
kind: Service
metadata:
  name: core-valid-refprog-data-bridge
  labels:
    app: gridcapa
    component: gridcapa-refprog-data-bridge
    region: core
    process: valid
spec:
  type: ClusterIP
  selector:
    app: gridcapa
    component: gridcapa-refprog-data-bridge
    region: core
    process: valid
  ports:
    - port: 80
      targetPort: 8080
      name: http
