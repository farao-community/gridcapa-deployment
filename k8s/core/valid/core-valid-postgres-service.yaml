apiVersion: v1
kind: Service
metadata:
  name: core-valid-postgres
  labels:
    app: gridcapa
    component: postgres
    region: core
    process: valid
spec:
  type: ClusterIP
  ports:
    - port: 5432
  selector:
    app: gridcapa
    component: postgres
    region: core
    process: valid