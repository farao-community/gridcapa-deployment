apiVersion: v1
kind: Service
metadata:
  name: core-valid-intraday-data-bridge
  labels:
    app: gridcapa
    component: data-bridge
    region: core
    process: valid-intraday
spec:
  type: ClusterIP
  selector:
    app: gridcapa
    component: data-bridge
    region: core
    process: valid-intraday
  ports:
    - port: 80
      targetPort: 8080
      name: http
