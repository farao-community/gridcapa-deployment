apiVersion: v1
kind: Service
metadata:
  name: core-valid-intraday-exporter
  labels:
    app: gridcapa
    component: exporter
    region: core
    process: valid-intraday
spec:
  type: ClusterIP
  selector:
    app: gridcapa
    component: exporter
    region: core
    process: valid-intraday
  ports:
    - port: 80
      targetPort: 8080
      name: http
