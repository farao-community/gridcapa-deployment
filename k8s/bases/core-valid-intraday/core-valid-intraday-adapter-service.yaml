apiVersion: v1
kind: Service
metadata:
  name: core-valid-intraday-adapter
  labels:
    app: gridcapa
    component: adapter
    region: core
    process: valid-intraday
spec:
  type: ClusterIP
  selector:
    app: gridcapa
    component: adapter
    region: core
    process: valid-intraday
  ports:
    - port: 80
      targetPort: 8080
      name: http
