apiVersion: v1
kind: Service
metadata:
  name: minio
  labels:
    app: gridcapa
    component: minio
spec:
  type: ClusterIP
  selector:
    app: gridcapa
    component: minio
  ports:
    - port: 9000
      name: browser
