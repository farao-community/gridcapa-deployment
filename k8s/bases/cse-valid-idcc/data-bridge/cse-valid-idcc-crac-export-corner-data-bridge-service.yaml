apiVersion: v1
kind: Service
metadata:
  name: cse-valid-idcc-crac-export-corner-data-bridge
  labels:
    app: gridcapa
    component: gridcapa-crac-export-corner-data-bridge
    region: cse
    process: valid-idcc
spec:
  type: ClusterIP
  selector:
    app: gridcapa
    component: gridcapa-crac-export-corner-data-bridge
    region: cse
    process: valid-idcc
  ports:
    - port: 80
      targetPort: 8080
      name: http
