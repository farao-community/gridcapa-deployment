kind: ConfigMap
apiVersion: v1
metadata:
  name: swe-idcc-gridcapa-job-launcher-configmap
  labels:
    app: gridcapa
    component: job-launcher
    region: swe
    process: idcc
data:
  application.yml: |-
    logback:
      amqp:
        exchange: swe-idcc-task-manager-logs-exchange
        routing-key: "#"
    spring:
      application:
        name: swe-idcc-job-launcher
      rabbitmq:
        host: gridcapa-rabbitmq
        username: ${RABBITMQ_USER}
        password: ${RABBITMQ_PASSWORD}
        port: 5672
      cloud:
        stream:
          bindings:
            default:
              group: swe-idcc-job-launcher
            run-task:
              destination: swe-idcc-run-task
            run-task-auto:
              destination: swe-idcc-run-task
            stop-task:
              destination: swe-idcc-stop-task
            consumeTaskDtoUpdate-in-0:
              destination: swe-idcc-task-updated
          rabbit:
            default:
              consumer:
                auto-bind-dlq: true
                max-attempts: 1
            bindings:
              consumeTaskDtoUpdate-in-0:
                consumer:
                  binding-routing-key: status
    
    job-launcher:
      auto: true
      url:
        task-manager-timestamp-url: http://swe-idcc-task-manager/tasks/
        task-manager-business-date-url: http://swe-idcc-task-manager/tasks/businessdate/
      process:
        tag: SWE_IDCC
        timezone: CET
    
    scheduler:
      enable: false


---

kind: Deployment
apiVersion: apps/v1
metadata:
  name: swe-idcc-gridcapa-job-launcher
  labels:
    app: gridcapa
    component: job-launcher
    region: swe
    process: idcc
spec:
  selector:
    matchLabels:
      app: gridcapa
      component: job-launcher
      region: swe
      process: idcc
  template:
    metadata:
      labels:
        app: gridcapa
        component: job-launcher
        region: swe
        process: idcc
    spec:
      containers:
        - name: gridcapa-job-launcher
          image: gridcapa-swe-job-launcher
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: RABBITMQ_USER
              valueFrom:
                secretKeyRef:
                  name: gridcapa-rabbitmq-credentials
                  key: rabbitmq-user
            - name: RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gridcapa-rabbitmq-credentials
                  key: rabbitmq-password
          volumeMounts:
            - mountPath: /config/application.yml
              subPath: application.yml
              name: swe-idcc-gridcapa-job-launcher-configmap-volume
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 25
            periodSeconds: 15
            timeoutSeconds: 2
            failureThreshold: 3
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 35
            timeoutSeconds: 2
            periodSeconds: 15
            failureThreshold: 3
            successThreshold: 1
          resources:
            requests:
              cpu: "0.1"
              memory: "384Mi"
            limits:
              cpu: "0.5"
              memory: "384Mi"
      restartPolicy: Always
      volumes:
        - name: swe-idcc-gridcapa-job-launcher-configmap-volume
          configMap:
            name: swe-idcc-gridcapa-job-launcher-configmap


---

apiVersion: v1
kind: Service
metadata:
  name: swe-idcc-gridcapa-job-launcher
  labels:
    app: gridcapa
    component: job-launcher
    region: swe
    process: idcc
spec:
  type: ClusterIP
  selector:
    app: gridcapa
    component: job-launcher
    region: swe
    process: idcc
  ports:
    - port: 80
      targetPort: 8080
      name: http
