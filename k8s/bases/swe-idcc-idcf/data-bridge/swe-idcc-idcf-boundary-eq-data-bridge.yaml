kind: ConfigMap
apiVersion: v1
metadata:
  name: swe-idcc-idcf-boundary-eq-data-bridge
  labels:
    app: gridcapa
    component: data-bridge
    file: boundary-eq
    region: swe
    process: idcc-idcf
data:
  application.yml: |-
    data-bridge:
      zone-id: "Europe/Paris"
      target-process: SWE_IDCC_IDCF
      file-type: BOUNDARY_EQ
      file-regex: (?<year>[0-9]{4})(?<month>[0-9]{2})(?<day>[0-9]{2})_(?<hour>[0-9]{2})(?<minute>[0-9]{2})_rr1_UX1_EQX.(xml|XML)
      time-validity: yearly
      remote-file-regex:
        - ".*.zip"
        - "(?<year>[0-9]{4})(?<month>[0-9]{2})(?<day>[0-9]{2})_(?<hour>[0-9]{2})(?<minute>[0-9]{2})_rr1_UX1_EQX.xml"
      sources:
        ftp:
          active: true
          host: ${SOURCE_HOST}
          port: ${SOURCE_PORT}
          username: ${SOURCE_USER}
          password: ${SOURCE_PASSWORD}
          base-directory: ${SOURCE_BASE_DIRECTORY}/boundaries
          polling-delay-in-ms: ${POLLING_DELAY}
          file-list-persistence-file: /data/gridcapa/ftp-metadata-store.properties
      sinks:
        minio:
          url: http://gridcapa-minio:9000
          access-key: ${MINIO_ACCESS_KEY}
          secret-key: ${MINIO_SECRET_KEY}
          bucket: ${MINIO_BUCKET}
          base-directory: ${MINIO_BASE_DIRECTORY}/BOUNDARIES


---

kind: Deployment
apiVersion: apps/v1
metadata:
  name: swe-idcc-idcf-boundary-eq-data-bridge
  labels:
    app: gridcapa
    component: data-bridge
    file: boundary-eq
    region: swe
    process: idcc-idcf

spec:
  selector:
    matchLabels:
      app: gridcapa
      component: data-bridge
      file: boundary-eq
      region: swe
      process: idcc-idcf
  template:
    metadata:
      labels:
        app: gridcapa
        component: data-bridge
        file: boundary-eq
        region: swe
        process: idcc-idcf
    spec:
      containers:
        - name: swe-idcc-idcf-boundary-eq-data-bridge
          image: gridcapa-swe-input-data-bridge
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: SOURCE_HOST
              valueFrom:
                configMapKeyRef:
                  name: env-configmap
                  key: GRIDCAPA_SOURCE_HOST_SWE_IDCC_IDCF
            - name: SOURCE_PORT
              valueFrom:
                configMapKeyRef:
                  name: env-configmap
                  key: GRIDCAPA_SOURCE_PORT_SWE_IDCC_IDCF
            - name: SOURCE_USER
              valueFrom:
                secretKeyRef:
                  name: gridcapa-ftp-credentials
                  key: ftp-user
            - name: SOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gridcapa-ftp-credentials
                  key: ftp-password
            - name: SOURCE_BASE_DIRECTORY
              valueFrom:
                configMapKeyRef:
                  name: env-configmap
                  key: GRIDCAPA_SOURCE_BASE_DIRECTORY_SWE_IDCC_IDCF
            - name: POLLING_DELAY
              valueFrom:
                configMapKeyRef:
                  name: env-configmap
                  key: GRIDCAPA_POLLING_DELAY_SWE_IDCC_IDCF
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: gridcapa-minio-credentials
                  key: minio-access-key
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: gridcapa-minio-credentials
                  key: minio-secret-key
            - name: MINIO_BUCKET
              valueFrom:
                configMapKeyRef:
                  name: env-configmap
                  key: GRIDCAPA_MINIO_BUCKET_SWE_IDCC_IDCF
            - name: MINIO_BASE_DIRECTORY
              valueFrom:
                configMapKeyRef:
                  name: env-configmap
                  key: GRIDCAPA_MINIO_BASE_DIRECTORY_SWE_IDCC_IDCF
          volumeMounts:
            - mountPath: /config/application.yml
              subPath: application.yml
              name: swe-idcc-idcf-boundary-eq-data-bridge-volume
            - mountPath: /data/gridcapa
              name: swe-idcc-idcf-boundary-eq-data-bridge-data
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 35
            timeoutSeconds: 2
            periodSeconds: 600
            failureThreshold: 3
            successThreshold: 1
          resources:
            requests:
              cpu: "0.1"
              memory: "384Mi"
            limits:
              cpu: "0.5"
              memory: "384Mi"
      restartPolicy: Always
      volumes:
        - name: swe-idcc-idcf-boundary-eq-data-bridge-volume
          configMap:
            name: swe-idcc-idcf-boundary-eq-data-bridge
        - name: swe-idcc-idcf-boundary-eq-data-bridge-data
          persistentVolumeClaim:
            claimName: swe-idcc-idcf-boundary-eq-data-bridge-storage


---

apiVersion: v1
kind: Service
metadata:
  name: swe-idcc-idcf-boundary-eq-data-bridge
  labels:
    app: gridcapa
    component: data-bridge
    file: boundary-eq
    region: swe
    process: idcc-idcf
spec:
  type: ClusterIP
  selector:
    app: gridcapa
    component: data-bridge
    file: boundary-eq
    region: swe
    process: idcc-idcf
  ports:
    - port: 80
      targetPort: 8080
      name: http
