kind: ConfigMap
apiVersion: v1
metadata:
  name: swe-idcc-idcf-exporter-configmap
  labels:
    app: gridcapa
    component: exporter
    region: swe
    process: idcc-idcf
data:
  application.yml: |-
    spring:
      application:
        name: swe-idcc-idcf-exporter
      rabbitmq:
        host: gridcapa-rabbitmq
        port: 5672
        username: ${RABBITMQ_USER}
        password: ${RABBITMQ_PASSWORD}
      cloud:
        stream:
          default:
            group: swe-idcc-idcf-exporter
          bindings:
            consumeTaskDtoUpdate-in-0:
              destination: swe-idcc-idcf-task-updated
          rabbit:
            default:
              consumer:
                auto-bind-dlq: true
                max-attempts: 1
            bindings:
              consumeTaskDtoUpdate-in-0:
                consumer:
                  binding-routing-key: status

    export:
      seperate-output-files: true
      unzip-files:
        - CGM_ESFR
        - CGM_FRES
        - CGM_PTES
        - CGM_ESPT

    task-manager:
      base-url: http://swe-idcc-idcf-task-manager
      fetch-task:
        reties-number: 6
        interval-in-seconds: 10

    ftp:
      active: true
      host: ${SOURCE_HOST}
      port: ${SOURCE_PORT}
      access-key: ${SOURCE_USER}
      secret-key: ${SOURCE_PASSWORD}
      remote-relative-destination-directory: ${SINK_DIRECTORY}
      retry-count: 3
      retry-sleep: 30

    logback:
      amqp:
        exchange: swe-idcc-idcf-task-manager-logs-exchange
        routing-key: "#"
