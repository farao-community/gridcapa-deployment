kind: CronJob
apiVersion: batch/v1
metadata:
  name: swe-d2cc-boundary-minio-automatic-purge
  labels:
    app: gridcapa
    component: boundary-minio-automatic-purge
    region: swe
    process: d2cc
spec:
  schedule: "0 0 * * *"
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 3
  successfulJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: swe-d2cc-boundary-minio-automatic-purge
              image: minio-mc
              command: ["/bin/sh","-c"]
              args:
                - rm -r --force --older-than 366d minio/gridcapa/SWE/D2CC/BOUNDARIES/;
                  rm -r --force --older-than 7d minio/gridcapa/SWE/D2CC/CGMs/;
                  rm -r --force --older-than 7d minio/gridcapa/SWE/D2CC/CRACs/;
                  rm -r --force --older-than 7d minio/gridcapa/SWE/D2CC/GLSKs/;
                  rm -r --force --older-than 7d minio/gridcapa/SWE/D2CC/XIIDM/;
                  find minio/gridcapa/SWE/D2CC/20* -type f -mtime +7 -delete;
                  find minio/gridcapa/SWE/D2CC/20* -type d -empty -delete;
              env:
                - name: MINIO_ACCESS_KEY
                  valueFrom:
                    secretKeyRef:
                      name: gridcapa-minio-credentials
                      key: minio-access-key
                - name: MINIO_SECRET_KEY
                  valueFrom:
                    secretKeyRef:
                      name: gridcapa-minio-credentials
                      key: minio-secret-key
                - name: MC_HOST_minio
                  value: http://$(MINIO_ACCESS_KEY):$(MINIO_SECRET_KEY)@gridcapa-minio:9000
          restartPolicy: Never
