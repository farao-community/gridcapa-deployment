apiVersion: v1
kind: Service
metadata:
  name: ce-cc-runner
  labels:
    app: gridcapa
    component: runner
    region: ce
    process: cc
spec:
  type: ClusterIP
  selector:
    app: gridcapa
    component: runner
    region: ce
    process: cc
  ports:
    - port: 80
      targetPort: 8080
      name: http
