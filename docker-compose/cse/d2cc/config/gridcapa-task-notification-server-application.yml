spring:
  cloud:
    stream:
      bindings:
        input:
          destination: task-notification
    config:
      enabled: false
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: gridcapa
    password: gridcapa
