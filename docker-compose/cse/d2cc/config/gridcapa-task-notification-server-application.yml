spring:
  cloud:
    stream:
      bindings:
        input:
          destination: task-notification
    config:
      enabled: false
  rabbitmq:
    addresses: rabbitmq:5672
    username: gridcapa
    password: gridcapa
