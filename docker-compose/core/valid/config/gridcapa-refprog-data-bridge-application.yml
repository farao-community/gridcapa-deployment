data-bridge:
  target-process: CORE_VALID
  file-type: REFPROG
  file-regex: [0-9]{8}_F110_.*.(xml|XML)
  time-validity: daily
  remote-file-regex:
    - ".*"
  sources:
    sftp:
      active: true
      host: sftp-server
      port: 22
      username: gridcapa
      password: gridcapa
      base-directory: /data/core/valid/refprogs
      polling-delay-in-ms: 5000
  sinks:
    minio:
      url: http://minio:9000
      access-key: gridcapa
      secret-key: gridcapa
      bucket: gridcapa
      base-directory: /CORE/VALID/REFPROGs