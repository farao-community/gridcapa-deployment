CREATE KEYSPACE IF NOT EXISTS config WITH replication = {'class': 'SimpleStrategy', 'replication_factor': 1};
CREATE TABLE IF NOT EXISTS config.parameters (
    userId text,
    appName text,
    name text,
    value text,
    PRIMARY KEY (userId, appName, name)
);