version: "3.7"
services:
  nginx-proxy:
    image: nginx:alpine
    ports:
      - 80:80
    volumes:
      - ./config/nginx.conf:/etc/nginx/nginx.conf
    networks:
      - gridcapa

  gridcapa-app:
    image: farao/gridcapa-app:latest
    networks:
      - gridcapa

networks:
  gridcapa:
