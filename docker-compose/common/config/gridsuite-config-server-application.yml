server:
  port: 8080

spring:
  rabbitmq:
    addresses: rabbitmq
    port: 5672
    username: gridcapa
    password: gridcapa
    virtual-host: gridcapa

powsybl-ws:
  database:
    host: gridcapa-postgres
    login: config_server
    password: config
    name: config