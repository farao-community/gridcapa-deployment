spring:
  cloud:
    stream:
      bindings:
        input:
          destination: core-valid-task-updated
          group: core-valid-task-notification
      rabbit:
        default:
          consumer:
            auto-bind-dlq: true
            max-attempts: 1
    config:
      enabled: false
  rabbitmq:
    addresses: rabbitmq:5672
    username: gridcapa
    password: gridcapa
