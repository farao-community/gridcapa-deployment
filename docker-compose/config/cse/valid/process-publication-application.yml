spring:
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: ${RABBITMQ_DEFAULT_USER}
    password: ${RABBITMQ_DEFAULT_PASS}

process-publication:
  messages:
    queue-name: timestamp-validation-requests
    expiration: 60000
