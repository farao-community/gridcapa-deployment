spring:
  rabbitmq:
    username: gridcapa
    password: gridcapa
    host: rabbitmq
    port: 5672
  cloud:
    stream:
      default:
        group: core-cc-fake-runner
      bindings:
        handleRun-in-0:
          destination: core-cc-run-task
        handleRun-out-0:
          destination: core-cc-task-status-updated
