spring:
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: gridcapa
    password: gridcapa
    template:
      reply-timeout: 6000000

rao-runner-server:
  amqp:
    queue-name: rao-request-queue

dichotomy-server:
  requests:
    queue-name: dichotomy-request-queue
  minio:
    access:
      name: gridcapa
      secret: gridcapa
    url: http://minio:9000/
    bucket: gridcapa
    base-path: ""
  security:
    whitelist:
      - http://minio:9000/